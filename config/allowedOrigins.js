// List of allowed origins (domains) for CORS (Cross-Origin Resource Sharing)
const allowedOrigins = ["https://technotes.onrender.com"]; // Domain allowed for cross-origin requests

module.exports = allowedOrigins;
